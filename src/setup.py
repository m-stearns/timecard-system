from setuptools import setup

setup(
    name="timecardsystem",
    version="0.1",
    packages=["timecardsystem"],
    include_package_data=True,
    install_requires=[
        'flask', 'wheel'
    ]
)